{
  "builds": [
    {
      "src": "Gemfile",
      "use": "@vercel/ruby",
      "config": {
        "buildCommand": "bundle install && bundle exec jekyll build"
      }
    }
  ],
  "outputDirectory": "_site"
}
